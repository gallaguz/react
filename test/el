React element

const element = {
    type: "button",
    props: {
        className: "button button-blue",
        children: {
            type: "b",
            props: {
                children: "OK!",
            },
        },
    },
}

DOM element

<button class="button button-blue">
    <b>OK!</b>
</button>